import type { ToastVariants } from './types';

export const EXIT_CLASS = 'toast-exit';
export const EXIT_DURATION = 1000;
export const DEFAULT_VARIANT: ToastVariants = 'info';
export const DEFAULT_AUTOHIDE_DURATION = 2500;
